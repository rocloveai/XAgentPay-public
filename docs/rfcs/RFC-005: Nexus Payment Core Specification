RFC-005: Nexus Payment Core Specification
来源指南
Nexus Payment Core 是网络中的状态仲裁中心与交易编排引擎，其核心使命是通过无状态编排将用户意图转化为可执行的链上交易。它通过 MCP 协议而非传统 API 对外提供服务，严格遵循事件溯源原则来确保订单生命周期的透明与可追溯。在运作流程上，该核心组件充当了各方的连接枢纽，通过集成外部风控结果来构建交易清单，并精细化管理从创建到结算的完整状态机。总而言之，它不直接触碰资金或决策风险，而是作为一个高效的中枢系统，确保支付流程在商户、风险网关和区块链之间无缝衔接。











# RFC-005: Nexus Payment Core Specification
| Metadata | Value |
| --- | --- |
| **Title** | Nexus Payment Core Specification |
| **Version** | 1.0.0 |
| **Status** | Standards Track (Draft) |
| **Scope** | Orchestration, State Management, Event Indexing |
## 1. Abstract (摘要)
Nexus Payment Core 是整个网络的**状态仲裁中心**与**交易编排引擎**。它不负责判断交易是否安全（这是 Gatekeeper 的职责），也不负责生成商业报价（这是 Merchant 的职责）。
它的核心职责是：**接收意图 -> 注入风控结果 -> 构建交易 -> 维护全生命周期状态 -> 对外提供 MCP 服务。**
## 2. Architecture & Design Principles (架构与原则)
* **Stateless Orchestration (无状态编排):** 编排过程只负责数据组装，不持有资金。
* **Event Sourcing (事件溯源):** 订单的最终状态变更必须基于链上 Event 或可验证的 Gatekeeper 信号。
* **MCP-First (智能体优先):** 所有能力通过 MCP Protocol 暴露，而非 REST API。
## 3. Process Logic: The Orchestration Pipeline (编排流水线)
当 UA 调用 `nexus_core_orchestrate` 时，Core 内部执行严格的流水线：
1. **Input Validation:** 校验商户 Quote 签名有效性，校验 NUPS v1.5 格式。
2. **Context Enrichment:** 提取 UA 的 IP、设备指纹、历史行为特征。
3. **Risk Challenge (External Call):**
* 同步调用 **RFC-006 Gatekeeper** 模块。
* 输入：`PayerContext + QuoteData`。
* 输出：`RiskPermit` (签名) 或 `BlockSignal` (拒绝)。
4. **Transaction Assembly:**
* 若风控通过，将 `RiskPermit` 编码进 `NexusRouter.pay` 的 Calldata。
* 生成全局唯一 `nexus_payment_id`。
5. **Manifest Generation:** 返回 `TransactionManifest` 给 UA。
## 4. State Machine Design (状态机设计)
Core 维护订单的生命周期，并作为 MCP Resource 暴露给各方。
* **CREATED:** 报价已接收，编排完成，等待 UA 签名。
* **AWAITING_TX:** UA 已获取 Manifest，Core 正在监听链上 Mempool。
* **BROADCASTED:** 监测到 `tx_hash`。
* **SETTLED:** 监测到链上 `PaymentProcessed` 事件 (不可逆)。
* **COMPLETED:** 商户通过 MCP `verify` 接口确认履约。
* **RISK_REJECTED:** Gatekeeper 拒绝签发 Permit。
* **TX_FAILED:** 链上执行 Revert。
## 5. MCP Interface Specification (对外接口)
### Tools (给 UA 调用)
* **`orchestrate_payment`**: 接收 Quote，返回 Manifest。
### Resources (给 MA/UA 订阅)
* **`nexus://core/orders/{id}`**: 实时状态流。
### Internal Hooks (给 Gatekeeper 回调)
* Core 提供 gRPC/Internal 接口供 Gatekeeper 在链下风控通过后，异步更新某些灰度名单状态。