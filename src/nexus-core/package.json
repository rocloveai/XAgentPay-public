{
  "name": "@nexuspay/nexus-core",
  "version": "0.1.0",
  "type": "module",
  "description": "NexusPay Core — payment orchestration types, constants, and repository layer",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "viem": "^2.46.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "@vitest/coverage-v8": "^3.0.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
